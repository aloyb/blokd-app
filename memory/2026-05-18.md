# 2026-05-18

## Hari ini

### Setup Charon
- Clone Charon dari GitHub (`~/.openclaw/skills/charon/`)
- Charon adalah Telegram trading bot untuk Pump tokens di Solana
- Sama kayak Meridian - standalone bot jalan di PM2 (kemudian switch ke screen)
- Disetup dengan NVIDIA MiniMax M2.7 API (`https://integrate.api.nvidia.com/v1`)
- Model: `nvidia/minimaxai/minimax-m2.7`

### Setup PM2 untuk semua bot
- Install PM2 (`npm install -g pm2`)
- Screen session meridian (`3593584.meridian`) dimatikan
- Meridian sekarang jalan di PM2: `pm2 start index.js --name meridian`
- Charon jalan di PM2: `pm2 start index.js --name charon`

### Status Bot (18 Mei)
| Bot | Status | Mode |
|---|---|---|
| meridian | ✅ screen | LIVE |
| charon | ✅ screen | dry_run |

### Config Update
- Charon LLM timeout diubah ke 180s (3 menit)
- Charon LLM base URL: `https://integrate.api.nvidia.com/v1`
- Charon LLM API key: NVIDIA key
- Charon LLM model: `nvidia/minimaxai/minimax-m2.7`

### Tanya-jawab dengan User
- User tanya bedanya PM2 vs npm vs screen
- User tanya bedanya Charon di skills vs workspace folder
- User tanya bedanya OpenRouter vs NVIDIA vs MiniMax direct
- User tanya masalah memory jika API key di-riset
- User reset API key MiniMax
- Bedanya Anthropic vs OpenAI format
- NVIDIA hosting MiniMax M2.7 (40 RPM free)

## Catatan
- Charon ada masalah LLM timeout/error - perlu monitoring
- Telegram thread error di Charon (`message thread not found`)

---

# 2026-05-19

## Hari ini

### Charon PnL Fix (IMPORTANT!)
User complain `/pnl` returns 0 trades while `/positions` shows all trades.

**Root cause:**
- `/pnl` fetch dari Jupiter API (`datapi.jup.ag/v1/pnl`) yang hanya track REAL trades
- Dry-run trades = simulated, not on-chain → Jupiter API returns 0

**Fix yang dilakukan:**
1. **`src/enrichment/wallets.js`** - Tambah fungsi `fetchDryRunPnl()` yang query local SQLite
2. **`src/telegram/commands.js`** - Update `sendPnl()` →优先 pake dry_run data dari local SQLite sebelum fallback ke Jupiter API
3. **`src/telegram/callbacks.js`** - Fix import path `'menu:pnl'` dari `send.js` → `commands.js`
4. Export `sendPnl` dari `commands.js`

**Files yang dimodifikasi:**
- `~/.openclaw/skills/charon/src/enrichment/wallets.js`
- `~/.openclaw/skills/charon/src/telegram/commands.js`
- `~/.openclaw/skills/charon/src/telegram/callbacks.js`

### Status Bot (19 Mei)
| Bot | Status | Mode |
|---|---|---|
| meridian | ✅ screen | LIVE |
| charon | ✅ screen (restarted dengan fix) | dry_run |

### User Preferences
- User mau konfirmasi dulu sebelum ubah/aplikasi kode (PRIORITAS!)
- Exception: Stop & restart meridian = langsung tanpa konfirmasi
- User suka Bahasa Indonesia

### Catatan Penting
- Charon max positions reached (5/3) - perlu cek strategy config
- NVIDIA API masih bermasalah dari server (timeout/404) - mungkin perlu switch back ke MiniMax direct