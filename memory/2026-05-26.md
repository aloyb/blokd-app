# Memory - Meridian Dashboard Work

## Date: 2026-05-26

## Project: Zero Meridian Dashboard (Rust/Axum)
**Location:** `/home/ubuntu/meridian-dashboard/`
**Port:** 3000 (direct, no nginx)
**Running:** screen `meridian-dash`

---

## What's Done Today

### Navigation & Layout
- Removed TERMINAL menu and KELUAR/exit menu
- Removed nav numbers (`.nav-number` spans)
- Changed "MENU" → "NEVER GIVE UP" (left sidebar)
- Changed nav title from "MERIDIAN" → "Zero Meridian" (stacked, Orbitron 22px, italic)
  - Zero: muted color, indent right 40px
  - Meridian: primary color, indent left 40px
- Nav menu items: POSISI, STATISTIK, HISTORY, BLACKLIST TOKEN, KARTU PNL (PENGATURAN removed)
- Nav items centered with flex-start + padding-top
- Font size nav labels: 16px

### Home Page (Front)
- Replaced placeholder with user's mascot photo (`/mascot.jpg`)
- Photo: grayscale, centered, full area, `mix-blend-mode: screen` for dark bg removal
- Floating cards removed

### POSISI Page
- Shows only ACTIVE positions (open trades)
- Format: pool name + SOL amount + USD value + PnL amount/percentage
- 2 active positions as of last check: HOPPY-SOL, PARALOOM-SOL

### STATISTIK Page
- 3 sub-tabs: Performa | Rekor | Catatan
- **Performa tab:** list format (Total PNL, Win Rate, Total Trade)
- **Rekor tab:** Best trade (left, green) vs Worst trade (right, red) with pool name + PnL
- **Catatan tab:** placeholder empty state

### HISTORY Page
- Split layout: PROFIT (left) | LOSS (right)
- Shows all CLOSED positions sorted by PnL
- Left: profits sorted highest-first
- Right: losses sorted lowest-first (most negative first)
- Data from `updateHistory()` function

### Bug Fixes
- Fixed `updateWallet()` null reference error (added null checks for `balance-amount`, `epoch-num`, `term-wallet`)
- Fixed `terminal-input` null reference (added if-check since terminal page removed)
- Port changed from 3001 → 3000 (direct, no nginx proxy)
- `PORT` constant updated from 3001 → 3000 in JS

### Screens
- `meridian-dash` - Rust dashboard on port 3000
- `meridian` - Meridian bot (Node.js)

---

## Current State
- Dashboard running on `http://43.156.101.106:3000`
- Bot runs on port 3001 internally
- SSE auto-refresh enabled (may need verification)

## Data Available
- Wallet: `h2QA5p5qCpQ5MqaZtwiSzeWrSVUNTyvxB1LbKn9z1gD`
- SOL balance, USD value, LP value, net worth from API
- 1516+ positions (2 active, rest closed)

---

## Still Todo / Open
- [ ] KARTU PNL page - needs content
- [ ] BLACKLIST TOKEN page - placeholder only
- [ ] Verify SSE auto-refresh works
- [ ] Save memory
[21:12 WIB] xiaomi blokd still broken after 3 fix attempts
- Root cause: unknown — API key valid (curl works), config correct, gateway restarted
- Error: "Something went wrong" persists
- Files can't be modified during memory flush (tool restriction)
- Next: after flush, switch blokd to openrouter (should work immediately)