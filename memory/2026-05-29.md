# 2026-05-29 Memory

- Meridian bot: `screeningModel=mimo-v2.5`, `managementModel=mimo-v2.5-pro`, `generalModel=mimo-v2.5-pro`; `llmModel` remains global fallback/log display and does not override role-specific screening model.
- Meridian screener stability: Xiaomi empty-response guard added for SCREENER; after 2 consecutive empty responses it falls back to deterministic compact `NO DEPLOY` instead of waiting through max steps.
- Meridian routing fix: manual Telegram `scan`, `scan lagi`, `scan again`, `scan now` now route to the same `runScreeningCycle` path as cron screening, preventing GENERAL agent from deploying via a different decision path.
- Meridian fib context: HOPPY-SOL and RICH-SOL both passed active fib gate because config is `5m OR 15m` (`requireAllIntervals=false`); HOPPY passed via 15m, RICH via 5m. If 15m-only, RICH would fail; if all TF required, both would fail.
- Meridian screening explanation: HOPPY/RICH absent from cron deploys was not fib failure; HOPPY was rejected by memory/OOR/low-yield context, RICH likely missed earlier candidate ranking/limit and appeared after HOPPY was manually deployed.
- Meridian notification fix: RICH close succeeded but Telegram close notif failed due HTML parse error from reason containing `<` (`fee/TVL 2.96% < min 15%`); `notifyClose()` now escapes `<`, `>`, `&` in reason/pair before sending.
- Meridian post-update hardening: added `npm run test:guards` via `test/test-regression-guards.js`; `npm test` now fails if update removes SL normalization, role model split, fib_0236 zone, manual scan routing to `runScreeningCycle`, close HTML escaping, trailing TP fixed-TP bypass, or volatility bin/strategy deploy guards.
- Meridian Xiaomi empty-response diagnosis: direct probe showed `mimo-v2.5` can spend small `max_tokens` entirely on hidden `reasoning_tokens` and return `content=""`/`finish_reason=length`; patched screener call to use full `screeningMaxTokens`/`maxTokens` budget (4096) instead of hardcoded 900 and added empty-message logging with finish_reason/completion/reasoning tokens.
[13:37 WIB] [decision] Meridian screener switched from Meteora to GMGN | kept same thresholds/logic: fib_0236 5m/15m OR, fee/TVL, TVL, bin_step 80-125, minTotalFee 25, top10 35%, bundler/bot 40%; bot restarted and GMGN polling active
[13:53 WIB] [decision] Meridian deploy/open-position output now includes final strategy, bins_below, volatility, and fib TF confirmation | patched manual /deploy and auto screener prompt/tool result; bot restarted and polling active
[14:12 WIB] [decision] Meridian fib_0236 data source switched to GMGN kline when screeningSource=gmgn | chart-indicators now calls GMGN `/v1/market/token_kline` for 5m/15m fib candles, keeps same 0.236-0.5 zone logic, verified probe source=gmgn and bot restarted
[17:38 WIB] [fix] Meridian removed pool `bin_step` hard gate | corrected confusion with deploy `bins_below`: screening/deploy no longer reject by pool bin_step; volatility still controls bins_below/strategy at open-position time; tests passed and bot restarted
[19:02 WIB] [decision] Meridian local restore contract added | AGENTS.md + MERIDIAN_LOCAL_RULES.md + npm run check:local-rules preserve GMGN/fib/bins/TP rules after updates | next: run checker after every pull/update
[20:04 WIB] [decision] Meridian fib source restored | fib_0236 now uses Meridian chart-indicators API again, not GMGN kline; local rules/checker updated and bot restarted | next: preserve this as post-update baseline
[22:31 WIB] [decision] Meridian fee/active-TVL relaxed | minFeeActiveTvlRatio changed from 0.1 to 0.05, local rules/checker updated, tests passed, bot restarted | next: monitor for more frequent but more borderline entries

[19:29 CST] [memory-flush] Meridian restore contract confirmed | canonical repo `/home/ubuntu/.openclaw/skills/meridian`; after update run `npm run check:local-rules && npm test`; guard files added: `AGENTS.md`, `MERIDIAN_LOCAL_RULES.md`, `scripts/check-local-rules.js`; do not edit workspace-level MEMORY/SOUL/TOOLS during flush
