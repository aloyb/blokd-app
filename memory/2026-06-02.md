# Memory - June 2, 2026

## Meridian Logic Changes (IN PROGRESS — bot stopped)

### Done:
1. **Fib zone narrowed**: fib_0236 entry zone changed from 0.236–0.5 → 0.236–0.382
   - File: `tools/chart-indicators.js` — `fib382` added to summary, fib_0236 case uses fib382 instead of fib50
   - File: `MERIDIAN_LOCAL_RULES.md` — updated

2. **BB upper gate added**: Entry now requires BOTH:
   - Fib zone (0.236–0.382) confirmed on 5m OR 15m
   - BB upper touch on 5m (close >= upperBand)
   - File: `tools/chart-indicators.js` — added BB 5m check after fib confirmation in `confirmIndicatorPreset`
   - File: `MERIDIAN_LOCAL_RULES.md` — updated

### Not yet changed (discussed):
- User aware of all 5 entry stages (screening, global fees, fib+BB entry, deploy safety, position management)
- User aware of close rules (Rule 1-5)
- User may want to adjust close logic next

### Bot status: STOPPED (screen meridian-bot killed)
### Restart command: `cd /home/ubuntu/.openclaw/skills/meridian && screen -dmS meridian-bot node index.js`

## Resume command
User says: **"lanjut meridian"** → load this context, check what else needs changing, then restart when ready.

## Meridian Hard Gate Fix (18:32 WIB)
- Problem: Bot deployed CATCH-SOL with fib_tf=none (fib indicator not confirmed)
- Root cause: fibTf calculated but NOT passed to deploy_position args; LLM could override screening
- Fix 1: Added `fib_tf: fibTf` to deploy_position call in index.js
- Fix 2: Added hard gate in executor.js — deploy_position blocked if fib_tf is "none" or missing
- Effect: LLM can no longer bypass fib indicator requirement
