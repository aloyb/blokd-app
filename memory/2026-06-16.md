[02:33 WIB] [fix] Meridian dashboard | changed History sort default to `Terbaru`, restyled `Terbaru/Jumlah` as two-column segmented controls, rebuilt release binary, restarted `meridian-dash` on port 3000.
[02:49 WIB] [tweak] Meridian dashboard | changed History segmented active color from green to light gray, rebuilt release binary, restarted `meridian-dash` on port 3000.
[02:58 WIB] [tweak] Meridian dashboard | adjusted History segmented control to medium gray with stronger border/shadow so columns remain visible, rebuilt release binary, restarted `meridian-dash`.
[03:09 WIB] [tweak] Meridian dashboard | renamed navbar/page `KARTU PNL` to `WAITLIST`, swapped navbar order so `WAITLIST` sits where `STATISTIK` was and `STATISTIK` moves lower, rebuilt release binary, restarted `meridian-dash`.
[03:17 WIB] [feature] Meridian dashboard | added `/api/waitlist` backed by `fib-waitlist.json`, rendered WAITLIST page as auto-refreshing candidate cards, rebuilt release binary, restarted `meridian-dash`.
[03:47 WIB] [security] Meridian dashboard | disabled free wallet login, added admin-only JWT guard, removed terminal HTTP/WS routes and frontend command sender, rotated JWT secret, rebuilt release binary, restarted `meridian-dash`; tests: login-wallet 404, terminal auth 404, admin state/waitlist 200.
[13:38 WIB] [fix] Meridian dashboard via trycloudflare showed empty menus | root cause static/index.html hardcoded API_BASE to hostname:3000; changed to window.location.origin, added no-store cache header in serve_index, rebuilt/restarted meridian-dash | verified tunnel API: 1879 positions, 1 active, 8 waitlist
[14:08 WIB] [decision] Meridian dashboard POSISI | active positions now source directly from Meteora DLMM portfolio API; local bot state is kept only for closed/history/performance; rebuilt release and restarted `meridian-dash`.
[14:16 WIB] [tweak] Meridian dashboard header | changed SOL icon to `◎`, replaced "saldo dompet aktif" label with shortened active wallet address from `/api/state.wallet.wallet`; rebuilt release and restarted `meridian-dash`.
[15:24 WIB] [tweak] Meridian dashboard mobile | tightened mobile header/sidebar/footer, hid desktop footer shortcuts on mobile, made History single-column compact so content is not covered; rebuilt release and restarted `meridian-dash`.
[15:44 WIB] [tweak] Meridian dashboard mobile | restored compact left rail/sidebar on mobile and changed History mobile back to two columns with independent PROFIT/LOSS scrolling; rebuilt release and restarted `meridian-dash`.
