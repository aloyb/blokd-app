[02:21 CST] [meridian] decision | patched screening finalization to append active fib waitlist summary (`Waitlist:` lines) to every Telegram Screening Cycle report; current active waitlist has WLM-SOL and two RKC-SOL pools; tests/local rules passed and bot restarted `1802402.meridian-bot` | next: next screening report should show waitlist tokens
[02:27 CST] [meridian] decision | added `/waitlist` to Telegram `setMyCommands` menu in addition to existing text handler; tests/local rules passed and bot restarted `1805564.meridian-bot`, command registration logged OK | next: operator can use `/waitlist` from Telegram command menu
[07:42 CST] [config] OpenClaw LLM routing | removed MiniMax from active OpenClaw/Zero/BlokD configs; default, Zero, and BlokD primary set to `xiaomi/mimo-v2.5-pro` with fallback `xiaomi/mimo-v2.5`; gateway restarted and status probe OK | next: monitor for Xiaomi runtime stability
[14:13 CST] [meridian] fix | diagnosed waitlist not opening: HONTER entered Fib zone once but deploy safety re-fetch hit `chart indicators 504` and blocked as `fib_tf=missing`; patched screener to prioritize `waitlist: READY` and pass the candidate's existing `indicator_confirmation`/`fib_tf` into `deploy_position`; tests passed and bot restarted `1978330.meridian-bot` | next: next successful indicator cycle should deploy ready waitlist instead of losing fib proof
