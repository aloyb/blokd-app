
## [12:42 GMT+8] [upgrade] Meridian Strategy Overhaul

### Changes Applied:
1. **Mixed Strategy (30/70)**: Deploy TWO positions per pool — 30% spot + 70% bid_ask
   - `config.strategy.strategy = "mixed"`, `mixedSpotPct = 0.3`
   - Screening cycle calls deploy_position TWICE (spot first, then bid_ask)
   - Duplicate pool guard relaxed: allows 2 positions in same pool for mixed
   - `maxPositions` raised to 4 (was 2) to accommodate 2 positions per pool

2. **FIB-based bins_below**: Uses fib 0.618-0.786 support zone instead of volatility
   - `config.strategy.useFibBins = true`
   - New `fibBinsBelow()` in chart-indicators.js calculates bin distance from entry to support
   - `extractFibSignal()` in fib-waitlist.js extracts fib levels from indicator confirmation
   - FIB bins auto-injected in screening cycle onToolStart handler

3. **Screening Source**: Switched to GMGN
   - `screeningSource = "gmgn"` (was "meteora")
   - `minMcap = 50000` (was 100000)
   - GMGN config also updated: minMcap=50000

4. **Entry**: Still uses fib_0236 preset (entry zone 0.236-0.382)

### Files Modified:
- `tools/chart-indicators.js` — added fibBinsBelow()
- `tools/executor.js` — mixed strategy safety checks, FIB bins_below, relaxed duplicate guard
- `tools/dlmm.js` — reject "mixed" as direct strategy (must resolve to spot/bid_ask)
- `config.js` — mixed strategy config, useFibBins, mixedSpotPct, reload support
- `prompt.js` — updated deploy rules for mixed strategy
- `fib-waitlist.js` — added extractFibSignal()
- `index.js` — updated screening prompt, FIB bins_below injection in onToolStart
- `user-config.json` — strategy=mixed, screeningSource=gmgn, minMcap=50000, maxPositions=4
- `gmgn-config.json` — minMcap=50000

### Bot Status:
- Screen: 2980533.meridian-bot
- Mode: LIVE, Model: mimo-v2.5-pro
- GMGN screening active (Stage1: 100 → 20 pass)
- 0 open positions, wallet: BsEpL7...992AC
[21:01 CST] [fix] Meridian mixed deploy | resolved min-deploy/session-lock bug: `_mixed_deploy` slices now allow 0.21/0.49 SOL and second add-liquidity deploy_position call with `existing_position_pubkey`; restarted screen `3102560.meridian-bot` | next monitor next eligible deploy
[22:01 CST] [fix] Meridian screening/deploy | switched active source back to Meteora, maxMcap=20M, maxTvl=300k; added deploy tool schema fields + executor inference so mixed 0.21/0.49 slices bypass 0.7 min even if `_mixed_deploy` is omitted; restarted screen `3120551.meridian-bot` | next eligible deploy should not fail on min 0.7
[22:49 CST] [deploy] Meridian GO-SOL | mixed deploy succeeded on position `7ZAtHNXs...6dtv`: 0.21 spot + 0.49 bid_ask, FIB bins_below=43, fib_0236 confirmed 5m/15m; live status shortly after: OOR upward active -502 > upper -505, PnL +0.08%, OOR 7m | formatter patched to report actual mixed result/FIB entry instead of fallback bins/?
